1. Use `ruff` to format the Python code.
2. Use `prettier` to format the Markdown files.
3. Do not install any packages in local environment (including pip or brew etc.), if you really need to use them, use virtual environment or docker to install them.
